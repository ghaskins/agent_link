{application, ${ARTIFACT},
[{description, ${DESCRIPTION}},
{id, ${ID}},
   {vsn, ${VERSION}},
   {modules, ${MODULES}},
   {registered, ${REGISTERED}},
   {mod, {'agent_link_app', []}},
   {applications, ${APPLICATIONS}}]}.
